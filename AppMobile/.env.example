# URL de base de l’API ChainCacao (variable lue au build via Expo).
# En production, utilisez HTTPS ; le cleartext Android n’est activé que pour les URLs http://.
EXPO_PUBLIC_API_URL=https://votre-api.example.com
